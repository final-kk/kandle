{
    "name": "kandle-monorepo",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "build": "pnpm -r run build",
        "test": "pnpm -r run test",
        "format": "prettier --write \"**/*.{ts,js,tsx,jsx,json,md}\"",
        "format:check": "prettier --check \"**/*.{ts,js,tsx,jsx,json,md}\"",
        "codegen": "npx tsx ./scripts/codegen/index.ts",
    },
    "devDependencies": {
        "@types/node": "^20.11.0",
        "@webgpu/types": "^0.1.68",
        "prettier": "^3.7.4",
        "tsconfig-paths": "^4.2.0",
        "typescript": "5.4.5"
    },
    "dependencies": {
        "bindings": "^1.5.0",
        "decimal.js": "^10.6.0"
    },
    "pnpm": {
        "overrides": {
            "typescript": "5.4.5"
        }
    }
}
